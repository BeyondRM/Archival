package brm.platform.screenui.window.selectable;
import java.awt.image.BufferedImage;


/**
 *
 * @author Gregory
 */
public class SelectableIconized extends APanelSelectable {
  public SelectableIconized(BufferedImage bi, boolean b0, boolean b1, boolean b2, int sx, int sy, int sz, int sw, int sh) {
    super(bi, b0, b1, b2, sx, sy, sz, sw, sh);
  }
}
