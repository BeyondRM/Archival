package brm.platform.screenui.window.container;
import brm.platform.screenui.window.AControl;
import java.awt.image.BufferedImage;


/**
 *
 * @author Gregory
 */
public class ContainerHorizontal extends AControlContainer {
  public ContainerHorizontal(AControl[] a, BufferedImage b, int x, int y, int z, int w, int h) {
    super(a, b, x, y, z, w, h);
  }
}
